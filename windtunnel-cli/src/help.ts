export const help = () => {
  console.log(`
  USAGE:
    windtunnel [COMMAND] [FILE]

  COMMANDS:
    test    Run all of the exported functions as tests
    time    Run all of the exported functions as benchmarks
  `);
};
